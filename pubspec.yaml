name: psj_portfolio_flutter_web
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  animations: ^2.0.0
  cupertino_icons: ^1.0.2

  # STATE MANAGEMENT
  get: 4.6.1
  
  # WIGETS
  page_transition: 2.0.4

  # UTILITIES
  responsive_framework: 0.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: 1.0.4
  mocktail: 0.1.4
  http_mock_adapter: 0.3.2
  flutter_launcher_icons: 0.9.2


flutter:
  uses-material-design: true

flutter_gen:
  output: lib/gen
  line_length: 120
  null_safety: true
  integrations:
    flutter_svg: true